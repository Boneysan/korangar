# Playtest feedback — 2026-09-05, first session with friends

> **Handoff (stopped mid T4):** [../2026-09-05-playtest-handoff.md](../2026-09-05-playtest-handoff.md).
> Working tree is uncommitted on both repos. Next work is a composed-geometry
> dump for facings 2/4/6, not new pixel offsets.

Four players, three remote over Tailscale. Everything below came from that
evening. Status is what I verified in code, not what I assume.

## Fixed

| Item | What it was |
|---|---|
| **Quest window never appears** | `Ctrl+Q` was bound **twice** — `ToggleQuestLogWindow` and `CloseTopWindow` fired in the same frame, so the log opened and immediately closed. It shipped 2026-08-25 and had never once been visible. Close moved to `Ctrl+W`. Not DM-only. |
| **Windows client white / no graphics** | Nothing chose the graphics backend: `adapter_score` scored only `device_type`, so the Vulkan and DX12 entries for one card tied and `max_by_key` let *enumeration order* decide. AMD now prefers Vulkan. **Needs a pack rebuild to reach anyone.** |

## Answered — these exist

- **`Alt+U` is not bound to anything.** Party is **Alt+Z** / **Alt+P**; quest log
  **Ctrl+Q**; inventory Alt+E; equipment Alt+Q; stats Alt+A; skills Alt+S;
  zeny/EXP HUD Alt+Shift+H; minimap Ctrl+Tab.
- **Auto-pickup exists**: `@autoloot <percent>`, and a button in the Commands
  window (Ctrl+O). The number is a **drop-rate threshold** — `@autoloot 1` takes
  only rare drops and leaves junk. Requires GM, so run it on a friend with
  `#autoloot "Char Name" 100`.
- **Client-only updates already work** — that is why the pack is split.
  `Seal-Cascade-Windows.zip` is 47 MB against 4 GB. **Not written down anywhere a
  friend can read**, which is the actual gap.

## Confirmed missing (feature requests, not bugs)

- Items/potions in the hotbar — skills only, no item support in the hotbar code.
- Multiple hotbar pages (Alt/Ctrl tiers) — one row, F1–F9, nine slots.
- WASD movement — `W`/`A`/`S` exist only for the debug camera and modifier chords.
- Names when hovering the minimap — blips only, no hit-testing.
- Teleport-to-party-member from the party window — `@jumpto` works but is GM-only.

## Bugs still to chase

**Head detaches on character select at facings 9 / 12 / 3.** Character select
draws characters as **world entities**, so this is the general attachment path
rather than a UI preview — and it may therefore affect the world too. **Do not
theorise about the offset.** Head placement has been guessed wrong five times on
this project; the only thing that has ever worked is printing the whole
computation per facing and letting the data name it. The creation preview
deliberately submits **one composed animation** so normal attachment rules hold
for all eight facings — compare against that first.

**Party window keeps showing the old job.** Wiring looks intact end to end:
Hercules calls `pc->update_job_and_level` from `pc_jobchange` (`pc.c:9019`), the
client registers `0x0ABD`, and its handler updates `job_id` **and** `class_name`.
So something subtler is wrong — watch the packet live rather than guess.

**Stat allocation may not apply.** BigZ reached base 3 with **54 unspent points
and every stat at 1**. Either the allocator was skipped at creation, or the
2026-09-04 replay fix does not fire. Needs one controlled character creation
with the allocator definitely used.

**`iz_ac01`: warps to a black screen, plus invisible walls.** Precedent exists —
`docs/MAP_TELEPORT_AUDIT.md` came from campaign teleports with bad coordinates.
Needs the specific warp coordinates from the player.

**Character creation text is too small on a 15" laptop.** Interface scale, not a
layout bug. Check whether the interface settings expose a usable scale.

**Headgear animation audit** — check every headgear renders and animates. A
systematic pass, not a quick check.

## Not a bug — decided

- **Party loot distribution stays random** (`party_item_share_type: 0`). Round
  robin is value 2. Operator preference, 2026-09-05.

## Bigger conversations

- Future direction for the game.

---

## Status after 2026-09-05 continuation

Codex started the session (logging, packaging docs, Troubleshoot.bat, creation-stat and party-job tests). This pass finished the player-facing work that was still open.

| Task | Status |
|---|---|
| T1 Rebuild packs | Still needed after this tree ships. Quest window + AMD white-screen were already committed; this tree adds logging, Troubleshoot.bat, update instructions, WASD, hotbar items/rows, party jump, minimap names. |
| T2 Stat allocation | **Not a live bug.** Headless `character-starting-stats` persists the Acolyte spread (1/3/9/9/7/1, 0 unspent). BigZ looking stuck was either skipping the allocator or a session before the 2026-09-04 replay fix. |
| T3 Party window old job | **Fixed on the server.** `pc_update_job_and_level` now writes `class` into the party cache and sends 0x302a so the char-server cache matches. 0x0ABD was already correct; a later full roster was overwriting Acolyte with Novice. |
| T4 Head detach at 9/12/3 | **Measured; the compositor is clean. No code changed, nothing to retune.** The composed dump ran (`playtest-sprite-audit composed`) and then a full sweep (`sweep`): **1,651,104 composed facings** across 196 job bodies × 42 hairs × both sexes. In the two standing groups — Idle (0) and ReadyFight (4), the only poses a character stands in — the head overlaps the body at **every** facing, with **zero** detachments. All 454 flagged facings are in Die / Hurt / Skill groups where the art itself leans away by 1–11 px. Attach coverage (`attach`): of 270,545 drawing motions in 597 player ACTs, the only standing motions with no attach point are 5 GM/non-player sprites, and those lack it at *all eight* facings, never a subset — so no playable job can lose parenting at some facings and keep it at others. `apply_child_attach` translates every head part by `body_attach − head_attach`, so head and body are locked together by construction; nothing between compose and draw can separate them (the shader's `mirror` flips UVs inside the part's own quad, and `extra_depth_offset` moves depth, not screen position). **What this does not prove:** the report was made against the pre-fix tree, and the loader's `attach_point_count == Some(1)` gate was removed in this same uncommitted tree. Needs one live look before it is called fixed — and in-world, not on the select cards, which are south-only now. |
| T5 `iz_ac01` | **Fixed.** Client GAT is 220×200 for 1F (matched) and **240×240** for 2F; server cache was a 2012 **200×240** `iz_ac02`. Rebuilt `iz_ac02.mcache` from `data.grf` and moved the 2F stairs from unwalkable (104,27) to (124,46), with returns at (126,48)/(132,48). Audit: 0 unsafe destinations. |
| T6 Interface scale | **Already existed.** Creation window now has a "Text size / interface scale" button. Menu > Interface Settings > Scaling, try 1.3 or 1.5. Ctrl+I. |
| T7 Update-client instructions | **Written** into both pack READMEs. |
| T8 Minimap hover names | **Done.** Party blips and the local player show a tooltip. |
| T9 Teleport to party member | **Done, not GM-only.** Party window "Go to" sends `@partyjump <name>`. Group 0 can use it; the target must be an online party member. |
| T10 Items on the hotbar | **Done.** Drag a potion from inventory onto a slot; 1–9 uses it. |
| T11 Hotbar pages | **Done.** Three rows of nine: 1–9, Ctrl+1–9, Alt+1–9. F1–F9 (and Ctrl/Alt+F) still work. |
| T12 WASD | **Done, optional.** Game Settings > WASD movement (on by default). Click-to-move remains. Camera-relative, 200 ms throttle. |
| T13 Headgear | **Done — hats render.** New `AccessoryName` library table reads `accessoryid.lub` + `accname.lub` (+ `accname_f.lub` for female overrides), and `get_entity_part_files` now emits the three headgear slots in bottom → middle → top order, directly after the head. **Hats parent to the body attach, exactly like the head** — measured, not assumed (`hat-attach`): a headgear ACT carries the *same* body-relative attach point the head does, so a hat is the head's **sibling**, not its child; parenting it to the head's attach would apply the head's own shift twice. Verified end to end with `hat-lookup`: **1706 of 1984** mapped view ids resolve to a real SPR+ACT pair; the rest are regional/promo hats these GRFs do not ship, and those add no layer rather than a fallback silhouette. A `ZC_SPRITE_CHANGE` on any hat slot now rebuilds the layers, and character-select cards show hats too (the character list already carries the three view ids). Not yet seen on screen. |

### Answers that are not tasks

- **Quest window** is for everyone, Ctrl+Q. It was opening and immediately closing because Ctrl+Q also meant close-window; close is now Ctrl+W.
- **Alt+U** is not bound. Party is Alt+Z / Alt+P.
- **Auto-pickup:** `@autoloot <percent>` (drop-rate threshold). Commands window Ctrl+O, or ask the host `#autoloot "Name" 100`.
- **Windows white screen:** backend tie-break; Troubleshoot.bat tries Vulkan then DX12 and now writes `troubleshoot-launch.log` even if Rust never starts.
- **Future plans:** see `docs/FEATURE_ROADMAP.md` — campaign/DM tools, classic effect coverage, then modernization (WASD is the first slice of that; action camera, skill-check dialogue, campfire, bestiary follow).

---

## Task breakdown

Sizes: **S** ≤ 1 hour · **M** ≤ half a day · **L** more than that.

### Do first — unblocks everyone

| # | Task | Size | Notes |
|---|---|---|---|
| **T1** | **Rebuild and reship both packs** | S | Carries the quest-window fix *and* the AMD white-screen fix. Until this happens neither reaches a single player. Also fold `Troubleshoot.bat` into the pack and add a troubleshooting line to both READMEs. |

### Bugs, in order of how often a player hits them

| # | Task | Size | What it needs |
|---|---|---|---|
| **T2** | Stat allocation may not apply at creation | S | One controlled character creation with the allocator definitely used, then read `status_point` in the DB. If still 48+, instrument the three stages. Affects **every new character**, so highest player impact. |
| **T3** | Party window shows the old job | S–M | Wiring is already traced and intact. Capture `0x0ABD` live with `KORANGAR_PACKET_LOG` around a job change. |
| **T4** | Head detaches at facings 9 / 12 / 3 | M | **Instrument, do not theorise** — print the per-facing offset computation. Compare against the creation preview, which composes one animation so attachment holds for all eight facings. |
| **T5** | `iz_ac01` warps to black screen, invisible walls | M | **Blocked on input**: needs the exact coordinates from whoever hit it. Then a teleport audit like `docs/MAP_TELEPORT_AUDIT.md`. |

### Small quality-of-life

| # | Task | Size | What it needs |
|---|---|---|---|
| **T6** | Interface too small on a 15" laptop | S | Check whether interface settings already expose a scale. If yes it is a documentation fix; if not, add one. |
| **T7** | Friend-facing "update the client" instructions | S | Docs only. The 47 MB half already exists; nothing tells a friend how to use it. |
| **T8** | Names when hovering the minimap | M | Hit-test the blips, reuse the party member name already held in state. |
| **T9** | Teleport-to-party-member button | S–M | Sends `@jumpto`. **GM only** — friends are group 0, so it would be a DM convenience, not a party feature. |

### Features — bigger, and worth deciding before building

| # | Task | Size | Notes |
|---|---|---|---|
| **T10** | Items/potions in the hotbar | M–L | The most-requested one. The hotbar is **server-side** (Hercules hotkey table) and already carries a type field, so this is client work rather than a protocol change. |
| **T11** | Multiple hotbar pages (Alt / Ctrl tiers) | M | Pairs naturally with T10 — nine slots is the actual complaint behind both. |
| **T12** | WASD movement | M | Needs a decision first: `W`/`A`/`S` currently serve the debug camera and modifier chords, and RO is click-to-move by design. |
| **T13** | Headgear animation audit | M | **Done.** Sweep plus rendering: `playtest-sprite-audit` (asset validity), `hat-attach` (parenting rule), `hat-lookup` (view id → sprite). |
