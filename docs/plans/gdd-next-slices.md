# GDD v0.2 — implementation plan for the next slices

**Current priority:** owner-directed completion of GDD Phase 1. The 20 September playtest list remains an acceptance source, especially for quests and DM Session; code changes can proceed, with live observations recorded before affected rows are marked complete. Slices 1–15 and S1–S10 sequence the broader Wave 1 work; Phase 1 scope is summarized in [GDD §19](../GDD.md#phase-1---foundational-ux--partial--paths-in-87-910-1017).

**Baseline:** [GDD §19](../GDD.md#v02-build-order--the-next-slices-sequenced-added-2026-09-21), implementation audit refreshed 2026-09-23 against both working trees. This plan covers client slices 1–15, server slices S1–S10, and the seven recommended design deliverables. It is a plan, not an implementation-status claim. Update the GDD audit and this table when a slice lands.

**Source of truth:** The GDD governs product behavior and scope; the [end-to-end improvement plan](gdd-improvement-plan.md) maps every in-scope GDD improvement and follow-on wave; this file governs Wave 1 build order and acceptance. The targeted specs below own data layouts and UI details. `FEATURE_ROADMAP.md`, `PROJECT_PLAN.md`, and old handoffs describe earlier milestones; their unchecked boxes are not a second queue for this GDD work. The Seal Cascade campaign remains a separate DM Session mode.

**Working rules:** Hercules owns gameplay, permissions, inventory, quests, and persistent discovery. Korangar may cache and display them. A slice that changes both forks is accepted only with both changes together. Use the existing 20220406 protocol unless the needed state cannot be expressed safely. Keep server changes configurable when they affect ordinary combat or progression. The explicit GDD exclusions (PvP/WoE, vending search, universal dodge, unrestricted world-map teleport) remain excluded.

## Decisions confirmed for this friends server

- Non-story persistent discoveries and general access unlocks belong to the **account**, not one character: encountered monsters, visited places, rumors, and general services use the same account identity. **Story quests, story chapter completion, choices, and story-gated access remain per character; every character must complete the story.** Character level/build/inventory also remain character state. A different account keeps its own general unlock record. S7 implements monster milestones first; each later unlock-bearing feature must classify itself as general/account or story/character.
- DM Session mode stores quest/flag state on each character. A character joining the active campaign party automatically receives the party's current campaign quests and story flags, without past rewards; a same-account alternate receives them only after joining. Disconnected party members catch up on return. See [DM party quest-sync contract](../specs/dm-party-quest-sync.md) and S10.
- The player Adventure Guide defaults to an **open, broadly searchable reference**. Monster, item, card, skill, job, map, and mechanics entries—and verified build-relevant details such as effects, requirements, drops, and rates—are available without a kill or lore check. An unlock adds discovery history, badges, and optional authored encounter notes; it does not hide mechanical facts. Campaign plot spoilers and DM-only controls remain separate.
- Skill and stat points can both be reset by ordinary players, **free at any level**, so experimentation is low-friction. These are target policies, not claims that both controls currently work. Keep single-skill refund subject to the existing prerequisite safety rules.

## Decisions still open

These are the GDD Appendix D questions and tuning choices that affect the numbered work. Proposed defaults let implementation proceed; the owner can change them before the named gate. A playtest value is recorded as a value to test, not a balance decision already made.

| Question | Proposed working answer | Decide by |
|---|---|---|
| Party EXP / level sync (Q5) | Effective `conf/import/battle.conf` already sets a 25% even-share bonus per additional member; retain and measure it. Test level spread 30 (currently 15) before choosing a permanent limit. Defer level sync until live evidence shows widened sharing fails. | S3 |
| Refinement severity (Q6) | Preserve current server behavior while odds and consequences are documented; review destructive outcomes with the friends group before a later refinement slice. | After 1–15 |
| Pilot maps and first MVP (Q7–Q8) | Use `prt_fild08`, `orcsdun01`, and `gl_prison`/`gl_knt01`; Eddga is the follow-on boss after pilot acceptance. | S5 and boss follow-on |
| Ping lifetime (Q9) | Pings expire after 15 seconds; map-specific pings disappear when the receiver leaves that map; a shared destination persists until accepted, replaced, or party ends. | 11 |
| Formula and data coverage (Q10–Q12) | Show only server-derived values that can be verified; label client formulas as estimates. Reuse existing UI windows; authored supplements cover script logic a generator cannot prove. | 8/9, then build planner |
| Fork-only packets and playtest priority (Q15, Q17) | Keep existing protocol additions documented as implementation support; prioritize inventory search, then pings, then tracker as the GDD build order states. Change the order if the next playtest names a sharper need. | Before scheduling 7/11/12 |
| Death recovery tuning (GDD §14.2) | Start with half the actual EXP lost refunded after 10 eligible same-map kills, or at the save point; record exact behavior for repeated deaths before coding. | S4 |

## Current implementation audit (code/config inspected 2026-09-23)

This is a source audit, not a claim that the feature has been seen on screen. Effective import configuration takes precedence over stock defaults.

| ID | Present today; remaining work |
|---|---|
| 1 | 2026-09-22: `ToastQueue` (two visible, coalesce by key, wait queue) is on `ClientState` and the HUD text line. Call sites that raise quest/level/drop/party notices, reduced-motion presentation, and a live UI pass are still open. |
| 2 | Both 20220406 cast packet handlers emit `SkillCast` with source, skill, and duration only; `Map::render_skill_footprint` exists. |
| 3 | `player_target.rs` is for player actions only. Monster click attacks; monster frame/chips and Tab cycling are unbuilt. |
| 4 | Character selection already has a two-step `WarningBanner`; Delete is reached from the right-click menu. Inventory drop actions exist on right-click; the Windows pack report still needs reproduction. |
| 5 | 2026-09-23: deterministic `tools/generate_navigation_graph.py` emits a graph from loaded static warp scripts (537 maps, 3,176 directed edges in the current artifact). Dynamic/conditional transports, full travel-service coverage, and live edge validation remain open. |
| 6 | 2026-09-23: the player-facing World Map renders a regional atlas of 27 selectable locations and highlights portal-reachable connections/routes. Selecting a destination draws a sampled walkable breadcrumb on the current minimap to the next portal and recalculates after a map change. `<NAVI>` tags remain stripped, destination information and visited-route state are missing, and live traversal has not been accepted. |
| 7 | 2026-09-23: inventory All/Equipped/Gear/Items tabs, inventory/storage name search, inventory sort/drag ordering persisted server-side, and per-character drop/sale protections are present in the working tree. Fine-grained category filters, storage reuse, split-to-stack server operation, and live acceptance remain open. |
| 8 | `export_skill_info.py` already produces per-level SP/range/cast/element/area/reagents for live hotbar/tree tooltips. A bestiary *merge* exporter exists; the original monster/item/card exporter is missing. Full category generation is unbuilt. |
| 9 | `interface/windows/dm/bestiary.rs` has search and a client-only Reveal all toggle. It is reachable from the unguarded Commands window even for regular players; `@monster` still has server permission checks. Unlocks are session state, and no dedicated player Guide exists. |
| 10 | `BufferedAction` holds walk-into-range attack/pickup/casts; no timed expiry/action-end trigger. |
| 11 | Party chat events go straight into chat history in `lib.rs`; no structured party-state parser, pings, or ready check. |
| 12 | 2026-09-23: Track/Untrack preferences persist per character in `game_settings.ron` and reconcile against the authoritative active quest list. General objective progress beyond server item/quest data, location routing, hunting goals, and live reconnect acceptance remain open. |
| 13 | Action chords are still in `input/mod.rs`; hotbar bindings are server-backed, which is a different feature from remapping controls. |
| 14 | `WindowCache` persists window geometry; lock, snap, named layouts, and combat fade are unbuilt. |
| 15 | UI scaling and theme loading exist. The named accessibility themes, reduced motion, combat-text controls, and density settings were not found. |
| S1 | 2026-09-22: both WoE includes in `npc/re/scripts_main.conf` are commented out. Still needs a map-server `--run-once` parse and a check that non-WoE guild scripts still load. |
| S2 | Group 0 grants `resetskill`/`refundskill`, but the registered full-reset command is `@skreset`, not the client's `@resetskill`; no alias was found. The client “Reset stats” button sends `@reset` (both stats and skills), while the stats-only command is `@streset` and group 0 lacks its grant. The priced `resetnpc.txt` is inactive; stock Hypnotist is limited to eligible first jobs below level 50 and zero inventory weight. Ordinary-player live verification and command/UI repair are due. |
| S3 | `conf/import/battle.conf` sets `party_even_share_bonus: 25`, overriding stock `party.conf: 0`; `party_share_level` is 15 in `conf/common/inter-server.conf`. |
| S4 | `exp.conf` has the stock death penalty. Campaign respawn/recovery settings are separate; no general death-EXP refund was found. |
| S5–S6 | Stock `mob_skill_db.conf`/mode machinery exists; no documented three-map archetype pilot or live results. |
| S7 | Client Bestiary unlocks are session-only; no per-account player unlock snapshot/delta. |
| S8 | `mob.c` already iterates nearby party members on a kill; `quest.c` checks party ID/active objectives. `AREA_SIZE` resolves to effective 30 cells. Live verification is due. |
| S9 | Stock AI exists; no opt-in skirmisher, preferred-range keeper, or hazard-aware path-cost layer found. |
| S10 | 2026-09-23: `OnPCPartyJoin`, `OnPCQuestLog`, and `OnPCLoadMapEvent` call `DM_CatchUpFromParty`; login sync waits for the quest log so it does not get overwritten. The helper copies the stored latest status for an explicit campaign quest/flag allowlist, including late joiners, without replaying past rewards. This is snapshot reconciliation, not a sequenced event log; allowlist coverage, character-scoped checkpoint mirrors, and live late-join/reconnect acceptance remain open. |

## Readiness and dependency gates

| Gate | Required result before dependent work | Check |
|---|---|---|
| G0 — current behavior | Recheck the 2026-09-21 GDD audit against the target branch before coding. Character deletion already has a two-step `WarningBanner` confirmation in `src/interface/windows/character_selection.rs`; slice 4 adds a visible affordance and the GDD's typed-name confirmation. Item dropping already has a right-click action; test the shipped Windows pack first. | One live reproduction of each reported issue; record pack revision and whether the action exists. |
| G1 — generated data | Inventory, target chips, navigation, and the Guide use versioned data generated from the *same* Hercules revision used to ship the server. `tools/export_skill_info.py` and `tools/extend_bestiary_export.py` exist, but the original bestiary/items/cards exporter is missing. Rebuild that source path before calling the encyclopedia automatic. | One command regenerates all supported categories; `--check`/diff fails on stale data; record source revision and counts. Existing consumers still deserialize. |
| G2 — real route coverage | Static `warp` lines under `Hercules/npc/re/warps/**` are only the first edge source. Include loaded custom warps and explicitly authored travel services; omit WoE, disabled scripts, instances, and conditional routes unless availability is modeled. Never route through an unverified edge. | Fixture paths Prontera → `prt_fild08`, Izlude → an accessible dungeon, and an unreachable destination. Each suggested exit exists and is traversable on the live server. |
| G3 — session transport | Party chat is an experiment for ephemeral pings/checks only. Verify its actual byte limit, rate limit, sender identity, and whether structured messages can be suppressed before chat rendering. The proposed control-character prefix does not give stock clients readable fallback text. If this cannot meet the contract, use a versioned fork packet. Persistent bestiary and quest state require server-originated sync, not peer messages. | Two Korangar clients plus a stock client; malformed/old message, reconnect, map change, and rate-limit checks. No structured text leaks to supported clients. |
| G4 — live acceptance | Unit/data checks establish extraction and state transitions; a live client pass establishes pixels, input, and server agreement. Use the existing headless harness for packet/server behavior, not as proof of UI appearance. | Record the client/server revision, scenario, observed result, and any GDD status change. |

## Client build order

The GDD numbers are stable IDs. Rows 1–4 can proceed without a server feature; 7 and 10 can proceed while the graph and data work runs. Dependencies below, not row adjacency, decide when a slice is ready to merge.

| ID | Implementation boundary and acceptance | Depends on |
|---|---|---|
| 1 Toast widget | One bounded queue in the existing HUD, reusable for quest completion, level-up, rare drops, and party events. Define priority, duplicate coalescing, expiry, max visible count, reduced-motion presentation, and a corresponding chat/log entry for missed notices. Verify simultaneous notices and UI scaling. | None |
| 2 Cast telegraphs | Preserve cast target cell/entity from the two `SkillCast` packet handlers through `NetworkEvent`; draw the server skill footprint only for area casts larger than one cell, during the authoritative cast interval. Cancel on interruption, map change, or actor removal. Unknown target/layout shows the cast bar only. Verify ground and entity-centered casts, cancellation, and no persistent decal. | G4; [combat animation spec](../specs/combat-animation-pipeline.md) |
| 3 Target cycling + monster frame | Tab/Shift+Tab cycle visible hostile entities in a stable distance/screen-order, skip invalid/dead/hidden targets, and preserve click targeting. `player_target.rs` is player-only, so add a monster-facing target surface keyed by entity/mob ID before showing race/size/element chips. Show verified fields without discovery gating; unknown fields stay labeled unknown. Verify two overlapping mobs, despawn, and a boss. | G1 for chips |
| 4 Destructive-action affordances | Put a visible Delete action on the character card and require typed character name before sending the existing delete event. Put a visible Drop action in inventory; keep quantity selection and confirm locked/rare items. Diagnose the Windows pack report before changing the drop packet path. Verify cancel, wrong name, and no network mutation before confirmation. | G0 |
| 5 Map graph generator | Produce a deterministic, versioned `maps`/`edges`/`source` JSON artifact from *loaded* static warp scripts, with per-edge source/destination cells and availability. Add authored transport edges with requirements; mark unsupported dynamic/instance edges. Validate map names/cells against the map index/cache, reject duplicates or broken references, and emit a coverage report. Route cost starts as hop count; distinguish unreachable from unknown. | G1, G2; [navigation spec](../specs/navigation-quest-guiding.md) |
| 6 `<NAVI>` and next exit | Parse the optional fields Hercules emits in `<INFO>` and render the label as an accessible action. Same-map targets mark an area/cell; cross-map targets choose the next verified exit from slice 5 and show a minimap marker/edge arrow. Recompute after a warp; clear or explain unavailable routes; never auto-walk across maps. Verify malformed tags render harmless text. | 5; [navigation spec](../specs/navigation-quest-guiding.md) |
| 7 Inventory controls | Add a shared item-list query model for inventory and storage: case-insensitive name search, category tabs from item type, stable client-side sort, and a per-character item lock list. Filter/sort never changes server slot indices or item counts; all actions resolve the original inventory index. Apply lock checks at drop and NPC sell dispatch, with a clear override. Verify duplicate stacks, stack splits, storage transfer, drag/drop, and a relog. | G1 for categories; G4 |
| 8 Encyclopedia data pipeline | Preserve the working per-level skill export/tooltips, add their missing delay/prerequisite fields, restore reproducible monster/item/card generation, then add maps, quests, NPCs, jobs, mechanics, refine odds, and effective server rules in stages. Each field has source, build revision, visibility policy, and `unknown` when scripts cannot be parsed safely; authored overrides are reviewed separately. Do not present raw item scripts or guessed formula prose as facts. See the [data contract](../specs/encyclopedia-data.md). | G1; map connections depend on 5 |
| 9 Player Adventure Guide | Give every player a visible launcher and one categorized search over monsters/items/cards **and existing exported skills** first; add jobs, maps/quests, status, mechanics, and rules as each category from 8 passes coverage checks. Search names, aliases, effects, source monsters, and other indexed terms; support category filters and cross-links. Show all verified build-relevant details immediately, including drops/rates and skill requirements; account discovery badges never gate them. Separate player and DM controls: the current Commands window and Reveal all toggle have no client permission gate. Cross-links retain context, offer Navigate only for known locations, and label incomplete fields. Verify a new non-DM account can research a build before any kill, plus login/reconnect, empty results, and unavailable data. | 8; 5 for Navigate; S7 only for discovery badges |
| 10 Single-action buffer | Extend `BufferedAction` to one replaceable action with a 200 ms starting expiry; fire after walk-into-range or the current local action ends if the server still permits it. Explicit movement/cancel, death, map change, lost target, and refusal clear it. Never replay a ground skill at an old cell after map change. Verify rapid attack→skill, skill→skill, latency, and expiry. | 2 for clear visual timing; G4 |
| 11 Party state + pings + ready check | Build one versioned, bounded session-message parser and carrier chosen at G3. Six ping kinds get map/cell, sender, expiry, and minimap/world rendering; a ready check has a unique nonce, timeout, and one reply per current member. Do not let peers write quest or bestiary state. Verify cross-map pings, stock-client fallback, replay/duplicate messages, membership changes, and chat cleanliness. | 1 for notices; 5 for shared destination routes; G3 |
| 12 Quest tracker + hunting goals | Track a persisted subset of quests from `src/state/quests.rs`/quest log; show server objective counts, completion, and link to the log/route. Add separately labeled client-only hunting goals pointing into the Guide. Persist per character, reconcile on login, and remove stale completed quests. Verify two characters, reconnect, and a quest whose objective arrives late. | 6 for routing, 9 for item/monster links; [campaign journal spec](../specs/campaign-quest-journal.md) |
| 13 Keybinding table | Extract actions/chords from `src/input/mod.rs` into versioned settings, with a remap screen, reserved-key rules, conflict resolution, reset, and import/export. Preserve the shipped default bindings and chat/NPC focus behavior. Verify keyboard layout differences, duplicate chords, and migration from current settings. | None; do before new HUD shortcuts |
| 14 HUD edit mode | Build on `WindowCache`: locked movement, edge snapping, reset, and named Classic/Modern/custom layouts. Version and migrate stored layouts per character. Add combat-only visibility/opacity driven by a clearly timed combat state; save/restore across resolution and UI-scale changes. Verify drag, lock, relog, profile switch, and combat fade. | 12 for tracker integration; [HUD spec](../specs/hud-edit-mode.md) |
| 15 Accessibility pass | Ship high-contrast and deuteranopia-safe themes, reduced flashing/shake, combat-text size/density, effect-density by source, and alternate ground-target confirmation. Telegraphs and hazard information remain readable at the lowest effect setting; color never carries meaning alone. Verify each setting in a crowded party fight at multiple UI scales. | 2, 13, 14; [GDD §15.3](../GDD.md#153-implementation-path-added-v02) |

**Client release gates:** After 1–4, run a short live feel pass; after 5–6, have a new player reach a named dungeon without a guide; after 8–9, look up an item and navigate item → monster → map; after 10–15, run a mixed party session with reduced effects. A green protocol suite does not close these gates.

## Server build order

The server track can proceed alongside client work. Use `Hercules/` paths below; exact generated source files are resolved when a slice starts.

| ID | Implementation boundary and acceptance | Depends on |
|---|---|---|
| S1 WoE scope | Remove the two WoE includes from `npc/re/scripts_main.conf`, leaving non-WoE guild functions intact. Check loaded script list and startup parse; confirm PvE maps/campaign still load. | None |
| S2 Free player stat and skill resets | Align the full-skill-reset command registration/grant/UI: Hercules registers `@skreset`, but group 0 grants `resetskill` and the client sends `@resetskill`; add a deliberate alias or switch both to `skreset`, retaining any existing working name. Grant group 0 the registered `streset` command and change the client “Reset stats” action from `@reset` (which resets both) to `@streset`. Put clearly labeled Stats / Skills resets in a player-facing build/stats/skills entry point, not solely the DM-style Commands window; expose `@refundskill <id>` in help. No fee, level, class, or empty-inventory prerequisite for these player controls. Confirm the new buttons do not spend Zeny or change the *other* point pool; test both at low/high level and across jobs, relog, and one-point refund prerequisite rejection as an ordinary player. Leave the inactive priced NPC untouched. | G4 |
| S3 Party share | Measure the **effective** 25% even-share bonus already set in `conf/import/battle.conf` (stock `party.conf` says 0 but is overridden). Test `party_share_level` 30 instead of the current 15 in `conf/common/inter-server.conf`, while retaining the existing bonus initially. Verify under/over the limit, nearby/far members, and total EXP before keeping a new limit. No level-sync feature is implied. | G4 |
| S4 Death recovery | Add a config-gated server script or hook that records actual base/job EXP lost per death, refunds half after 10 eligible same-map kills or a save-point visit, and never refunds twice. Test relog, repeated death, zero-EXP, and party-kill behavior before shipping. Verify ledger/EXP values, not just text. | G4 |
| S5 Early AI pilot | Use `mob_skill_db`/modes on `prt_fild08` for one readable Aggressor and Coward pattern, with restrained cast timing. Record baseline spawn/kill rates and an easy rollback. Verify normal farming is still quick and telegraphs match server cells. | 2; [AI pilot plan](../specs/monster-ai-pilot.md) |
| S6 Mid/late AI pilot | Extend data-only family templates to `orcsdun01`, then `gl_prison`/`gl_knt01`; keep the Orc Archer keeper behavior for S9. Validate each tier before advancing, including solo and mixed-party pressure. | S5; [AI pilot plan](../specs/monster-ai-pilot.md) |
| S7 Account discovery state | Store monster discovery milestones per account on Hercules, including validated migration/merge from any client cache; emit bounded authoritative snapshots on login and deltas on kills/discovery. The open Guide works before and during sync; discovery badges/notes reconcile afterward, without hiding build data. Test two characters on one account, another account, reconnect, and no DM permissions. Campaign-only spoilers remain separate. | 8/9 data IDs; server-originated transport from G3, never peer trust; [persistence spec](../specs/bestiary-unlock-persistence.md) |
| S8 Shared stock quests | **Already implemented in code:** `mob.c` calls `quest->update_objective_sub` over `AREA_SIZE` for the killer's party; `quest.c` checks party ID and active quest. Effective radius is 30 cells via `conf/import/battle.conf`. Live-verify solo, mixed quest ownership, range boundary, duplicate killer credit, and simultaneous kills. Only add a separate quest-credit range/config key if this shared draw-distance radius fails the playtest. Campaign `DM_Party*` wrappers remain unchanged. | G4 |
| S9 C-side AI | Implement Skirmisher/keeper distance states and hazard-aware path cost only for opted-in monster profiles. Bound extra path searches and prevent oscillation/stuck loops; restore stock behavior through config or per-mob flag. Pilot Orc Archers first, then late-map monsters, never all mobs at once. | S5/S6 live feedback; [AI pilot plan](../specs/monster-ai-pilot.md) |
| S10 DM party quest sync | Preserve online party helpers; persist typed quest/flag transitions and replay missed events on reconnect/map load. On joining the active campaign party, snapshot the current quest/flag state to that character without granting old rewards. Replace account-scoped checkpoint mirrors with character-scoped state. Reconcile idempotently and report divergence. Verify online updates, disconnect/rejoin, late join, same-account alternate before/after it joins, character switch, party re-creation, repeated sync, and failed replay. | G4; [DM party quest-sync contract](../specs/dm-party-quest-sync.md) |

**Server checks:** Script/data slices need `script-checker` where applicable and a `map-server --run-once` parse. Config and C slices need the server build plus a live 20220406 client/headless scenario. Before shipping rate or reward changes, capture before/after EXP, Zeny, and time-to-kill from a small party session.

**DM Session gate:** Schedule S10 before calling reconnect/late-join campaign synchronization complete or shipping story arcs that rely on it. The existing online wrappers remain usable for a continuously connected party, but their success is not evidence of offline replay, late-join snapshots, or character isolation.

## Design deliverables and work left after these slices

| GDD deliverable | Where implementation detail lives | Exit condition |
|---|---|---|
| Tactical Combat Specification | [combat-input contract](../specs/gdd-combat-input.md), [combat animation spec](../specs/combat-animation-pipeline.md), [AI pilot plan](../specs/monster-ai-pilot.md) | Target/cast/cancel state transitions and telegraph eligibility are testable on live skills. Later skill-range preview, damage cue, and client acknowledgement get their own slices. |
| UI Wireframe Pack | [next-slice wireframes](../specs/gdd-ui-wireframes.md) and [HUD spec](../specs/hud-edit-mode.md) | Use the documented hierarchy/states; capture screenshots and refine spacing/visuals in each UI slice PR. |
| Navigation Data Specification | [navigation spec](../specs/navigation-quest-guiding.md), rows 5/6, G2 | Graph schema, source filtering, dynamic-route behavior, and fixtures pass. World map, portal labels, and population overlays follow as separately tracked GDD §9.10 work. |
| Monster AI Behavior Library | [AI pilot plan](../specs/monster-ai-pilot.md), S5/S6/S9 | Reusable family profiles and rollback parameters exist; each pilot has a live observation log. |
| Pilot Region Design | `prt_fild08` in [AI pilot plan](../specs/monster-ai-pilot.md) | One early area validates readable combat and next-exit guidance together; quests/Guide links are added only once their source data exists. |
| Encyclopedia Data Generator Specification | [data contract](../specs/encyclopedia-data.md), G1, rows 8/9 | Regeneration and drift check pass; unsupported fields are visible as unknown, not silently fabricated. |
| Implementation Status Refresh | GDD Appendix E and this plan | After each live gate, update rows with actual evidence and date. |

**Follow-on queue:** The [end-to-end improvement plan](gdd-improvement-plan.md#follow-on-implementation-queue) assigns F01–F39 to the remaining GDD work: combat previews/explanations, build planning, world/rumors, story, bosses, social/UI, loot/economy/crafting, accessibility, and telemetry. These are not silently included in 1–15/S1–S10. Later unlocks must follow the account-general versus character-story split above; unresolved balance/content choices have explicit decision gates in the master plan.
